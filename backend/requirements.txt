fastapi>=0.115,<1.0
uvicorn[standard]>=0.30
pydantic>=2.9,<3.0
pydantic-settings>=2.5
supabase>=2.7
python-jose[cryptography]>=3.3
python-multipart>=0.0.18
pytest>=8.0
httpx>=0.27,<1

# --- OCR / soil-report extraction ---
# Requires the system packages `tesseract-ocr` and `poppler-utils` to be
# installed (apt-get install tesseract-ocr poppler-utils). See
# app/ocr/engine.py for engine-selection rationale.
pytesseract>=0.3.10
pdf2image>=1.17
pdfplumber>=0.11
Pillow>=10.0
pytz>=2024.1

# --- Geospatial / satellite ingestion ---
# Required by app/services/satellite_ingestion_service.py (Open-Meteo +
# Sentinel Hub HTTP calls) and backend/scripts/ingest_real_data.py.
requests>=2.31,<3
